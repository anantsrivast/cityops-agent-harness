# Phase 01: Self-Improving Copilot Notebook Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build `notebooks/01_self_improving_copilot_complete.ipynb` (+ generated `_todo` flavor): the CityOps copilot gains a meaning-retrieved tool registry, an agent loop with full-trajectory capture, LLM-judge-verified workflow success, gray-band intent dedup, and a skill harvester with a provisional→approved/retired lifecycle — closing the Path-2 gaps from `memory_promotion_review.md`.

**Architecture:** Pure decision logic lives in `src/cityops_harness/improve.py` (unit-tested offline); the notebook is generated by `tools/make_01_notebook.py` and contains DB/LLM glue importing those helpers. Domain bootstrap (CITY_ASSET, CITY_INSPECTION_FINDING, in-DB embedder) is idempotent pre-built code adapted from the CityOps lab; the new registries use a `HARNESS_` table prefix. The Agent Memory SDK is NOT used in this notebook (deferred to 02).

**Tech Stack:** Python 3.12, python-oracledb (VECTOR(384) + `array('f',...)` binds), LangChain `bind_tools`/`with_structured_output` (provider-portable via `cityops_harness.llm.chat_model`), Langfuse scores (guarded), nbformat.

## Global Constraints

- Working dir `/Users/anantsr/Documents/Projects/total_recall`, branch `foundation`. Venv `.venv/` (Python 3.12 via uv); use `.venv/bin/pytest` / `.venv/bin/python`.
- **Never touch** `notebook_complete_ollama.ipynb` or `total_recall_complete (1).ipynb`. Never stage `wallet/`, `.env`, `.venv/`.
- All pytest tests run **without** DB, LLM, or network.
- Exact API facts (SDK, lab code, oracledb patterns) are in `.superpowers/sdd/phase01-api-reference.md` — implementers of notebook tasks must read it; do not invent SDK calls.
- New registry tables use `HARNESS_` prefix: `HARNESS_TOOLS`, `HARNESS_WORKFLOW`, `HARNESS_SKILLS` (the shared `ANANT` schema hosts other projects — never `DROP` anything not `CITY_`/`HARNESS_`-prefixed). Domain tables keep the lab names `CITY_ASSET`, `CITY_INSPECTION_FINDING` (idempotent create).
- Embeddings: in-database ONNX `ALL_MINILM_L12_V2`, 384-dim, `array.array('f', embedder.embed([text])[0].tolist())` bind pattern.
- Learner-gap convention (from `tools/make_student.py`): `# ✏️ TODO(n): <instruction>` line, then `# TODO-SOLUTION-START` / `# TODO-SOLUTION-END` around solution lines. Five TODOs in this notebook.
- Thresholds (exact values, used consistently): intent dedup `merge_below=0.15`, `new_above=0.40`; tool retrieval `TOOL_DISTANCE_MAX=0.75`; skill retrieval `SKILL_DISTANCE_MAX=0.55`; lifecycle `promote_at=2` verified uses (0 failures), `retire_failures=2`; harvest `min_occurrences=3` and `verified_successes >= 2` and `verified_successes > failures`.
- Notebook files: `notebooks/01_self_improving_copilot_complete.ipynb` (generated) + `notebooks/01_self_improving_copilot_todo.ipynb` (via `tools/make_student.py`). Kernelspec `harness` like 00.
- Commit messages end with `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`.

---

### Task 1: Skill document + trajectory helpers (`improve.py`, part 1)

**Files:**
- Create: `src/cityops_harness/improve.py`
- Test: `tests/test_improve_render.py`

**Interfaces:**
- Produces: `render_skill_md(*, name, description, tools, when_to_use, steps_body, source_workflow_id, schema_sha) -> str`; `parse_skill_md(text) -> dict` (keys `name, description, tools (list), source_workflow, schema_sha, when_to_use, steps_body`; raises `ValueError` on malformed); `truncate_result(value, max_chars=300) -> str`; `trajectory_to_text(steps, max_result_chars=300) -> str` where a step is `{"tool": str, "args": dict, "result": str}`.

- [ ] **Step 1: Write the failing tests**

`tests/test_improve_render.py`:

```python
import pytest

from cityops_harness.improve import (
    parse_skill_md,
    render_skill_md,
    trajectory_to_text,
    truncate_result,
)


def _render():
    return render_skill_md(
        name="corrosion-triage",
        description="Triage a corrosion report against history and record a finding",
        tools=["find_similar_findings", "log_finding"],
        when_to_use="An inspector reports corrosion and needs it assessed and recorded.",
        steps_body="1. Search history.\n2. Grade severity.\n3. Record the finding.",
        source_workflow_id="wf-123",
        schema_sha="abc123",
    )


def test_render_roundtrips_through_parse():
    parsed = parse_skill_md(_render())
    assert parsed["name"] == "corrosion-triage"
    assert parsed["tools"] == ["find_similar_findings", "log_finding"]
    assert parsed["source_workflow"] == "wf-123"
    assert parsed["schema_sha"] == "abc123"
    assert "Search history" in parsed["steps_body"]
    assert "inspector reports corrosion" in parsed["when_to_use"]


def test_parse_rejects_missing_frontmatter():
    with pytest.raises(ValueError, match="frontmatter"):
        parse_skill_md("## Steps\n1. do it\n")


def test_parse_rejects_missing_steps():
    text = _render().split("## Steps")[0]
    with pytest.raises(ValueError, match="Steps"):
        parse_skill_md(text)


def test_truncate_result_short_passthrough():
    assert truncate_result("ok", 300) == "ok"


def test_truncate_result_long_appends_marker():
    out = truncate_result("x" * 500, 300)
    assert out.startswith("x" * 300)
    assert "truncated 200 chars" in out


def test_trajectory_to_text_orders_and_truncates():
    steps = [
        {"tool": "find_similar_findings", "args": {"description": "rust"}, "result": "y" * 400},
        {"tool": "log_finding", "args": {"severity": "high"}, "result": "fid-1"},
    ]
    text = trajectory_to_text(steps, max_result_chars=100)
    assert text.index("Step 1: find_similar_findings") < text.index("Step 2: log_finding")
    assert "truncated 300 chars" in text
    assert '"severity": "high"' in text
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `.venv/bin/pytest tests/test_improve_render.py -q`
Expected: FAIL with `ModuleNotFoundError: No module named 'cityops_harness.improve'`

- [ ] **Step 3: Implement `src/cityops_harness/improve.py`**

```python
"""Pure decision logic for the self-improving harness (notebook 01).

Everything here is side-effect free so it can be unit-tested without a
database or an LLM. The notebook supplies the SQL and model calls.
"""

from __future__ import annotations

import hashlib
import json

_SKILL_TEMPLATE = """---
name: {name}
description: {description}
tools: {tools}
source_workflow: {source_workflow_id}
schema_sha: {schema_sha}
---

## When to use
{when_to_use}

## Steps
{steps_body}
"""


def render_skill_md(
    *,
    name: str,
    description: str,
    tools: list[str],
    when_to_use: str,
    steps_body: str,
    source_workflow_id: str,
    schema_sha: str,
) -> str:
    return _SKILL_TEMPLATE.format(
        name=name,
        description=description,
        tools=", ".join(tools),
        source_workflow_id=source_workflow_id,
        schema_sha=schema_sha,
        when_to_use=when_to_use.strip(),
        steps_body=steps_body.strip(),
    )


def parse_skill_md(text: str) -> dict:
    if not text.startswith("---"):
        raise ValueError("skill document missing frontmatter")
    try:
        _, front, body = text.split("---", 2)
    except ValueError as exc:
        raise ValueError("skill document missing frontmatter close") from exc

    meta: dict = {}
    for line in front.strip().splitlines():
        key, _, value = line.partition(":")
        meta[key.strip()] = value.strip()
    for required in ("name", "description", "tools", "source_workflow", "schema_sha"):
        if required not in meta:
            raise ValueError(f"skill frontmatter missing {required!r}")
    meta["tools"] = [t.strip() for t in meta["tools"].split(",") if t.strip()]

    if "## Steps" not in body:
        raise ValueError("skill document missing '## Steps' section")
    when_part, steps_part = body.split("## Steps", 1)
    meta["when_to_use"] = when_part.replace("## When to use", "").strip()
    meta["steps_body"] = steps_part.strip()
    return meta


def truncate_result(value: str, max_chars: int = 300) -> str:
    value = str(value)
    if len(value) <= max_chars:
        return value
    return f"{value[:max_chars]} ...[truncated {len(value) - max_chars} chars]"


def trajectory_to_text(steps: list[dict], max_result_chars: int = 300) -> str:
    lines = []
    for i, step in enumerate(steps, start=1):
        lines.append(f"Step {i}: {step['tool']}")
        lines.append(f"  args: {json.dumps(step['args'], default=str)}")
        lines.append(f"  result: {truncate_result(step['result'], max_result_chars)}")
    return "\n".join(lines)
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `.venv/bin/pytest tests/test_improve_render.py -q`
Expected: 6 passed

- [ ] **Step 5: Commit**

```bash
git add src/cityops_harness/improve.py tests/test_improve_render.py
git commit -m "feat: skill document rendering and trajectory text helpers

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 2: Decision helpers (`improve.py`, part 2)

**Files:**
- Modify: `src/cityops_harness/improve.py` (append)
- Test: `tests/test_improve_decisions.py`

**Interfaces:**
- Consumes: nothing beyond Task 1's module.
- Produces: `merge_decision(distance, merge_below=0.15, new_above=0.40) -> str` (`"merge" | "ask" | "new"`); `lifecycle_transition(status, verified_successes, failures, promote_at=2, retire_failures=2) -> str` over statuses `"provisional" | "approved" | "retired"`; `harvest_ready(occurrences, verified_successes, failures, min_occurrences=3) -> bool`; `filter_by_distance(rows, max_distance, key="distance") -> list[dict]`; `compute_schema_sha(columns) -> str` (sha256 hex over sorted `"table.column:type"` strings; `columns` is a list of `(table, column, dtype)` tuples).

- [ ] **Step 1: Write the failing tests**

`tests/test_improve_decisions.py`:

```python
from cityops_harness.improve import (
    compute_schema_sha,
    filter_by_distance,
    harvest_ready,
    lifecycle_transition,
    merge_decision,
)


def test_merge_decision_bands():
    assert merge_decision(0.05) == "merge"
    assert merge_decision(0.15) == "ask"      # boundary joins the gray band
    assert merge_decision(0.30) == "ask"
    assert merge_decision(0.40) == "ask"      # boundary still gray
    assert merge_decision(0.41) == "new"


def test_lifecycle_promotes_provisional_after_clean_verified_uses():
    assert lifecycle_transition("provisional", 2, 0) == "approved"
    assert lifecycle_transition("provisional", 1, 0) == "provisional"


def test_lifecycle_never_promotes_with_failures():
    assert lifecycle_transition("provisional", 5, 1) == "provisional"


def test_lifecycle_retires_on_failures_from_either_status():
    assert lifecycle_transition("provisional", 0, 2) == "retired"
    assert lifecycle_transition("approved", 9, 2) == "retired"


def test_lifecycle_retired_is_terminal():
    assert lifecycle_transition("retired", 10, 0) == "retired"


def test_harvest_ready_requires_recurrence_and_verified_majority():
    assert harvest_ready(3, 2, 0) is True
    assert harvest_ready(2, 2, 0) is False    # not recurrent enough
    assert harvest_ready(5, 1, 0) is False    # not enough verified successes
    assert harvest_ready(5, 2, 2) is False    # not a verified majority


def test_filter_by_distance():
    rows = [{"name": "a", "distance": 0.2}, {"name": "b", "distance": 0.9}]
    assert filter_by_distance(rows, 0.5) == [{"name": "a", "distance": 0.2}]
    assert filter_by_distance(rows, 0.1) == []


def test_compute_schema_sha_order_independent():
    a = compute_schema_sha([("T1", "C1", "VARCHAR2"), ("T2", "C2", "NUMBER")])
    b = compute_schema_sha([("T2", "C2", "NUMBER"), ("T1", "C1", "VARCHAR2")])
    assert a == b and len(a) == 64


def test_compute_schema_sha_changes_with_schema():
    a = compute_schema_sha([("T1", "C1", "VARCHAR2")])
    b = compute_schema_sha([("T1", "C1", "CLOB")])
    assert a != b
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `.venv/bin/pytest tests/test_improve_decisions.py -q`
Expected: FAIL with `ImportError` (names not defined)

- [ ] **Step 3: Append to `src/cityops_harness/improve.py`**

```python
def merge_decision(
    distance: float, merge_below: float = 0.15, new_above: float = 0.40
) -> str:
    """Gray-band intent dedup: certain merges and certain news are decided by
    embedding distance alone; the band in between gets an LLM same-task check."""
    if distance < merge_below:
        return "merge"
    if distance <= new_above:
        return "ask"
    return "new"


def lifecycle_transition(
    status: str,
    verified_successes: int,
    failures: int,
    promote_at: int = 2,
    retire_failures: int = 2,
) -> str:
    """Skill lifecycle: provisional skills earn approval through verified,
    failure-free linked outcomes; accumulated failures retire any skill."""
    if status == "retired":
        return "retired"
    if failures >= retire_failures:
        return "retired"
    if status == "provisional" and verified_successes >= promote_at and failures == 0:
        return "approved"
    return status


def harvest_ready(
    occurrences: int,
    verified_successes: int,
    failures: int,
    min_occurrences: int = 3,
) -> bool:
    """Promote only what recurs AND verifiably works."""
    return (
        occurrences >= min_occurrences
        and verified_successes >= 2
        and verified_successes > failures
    )


def filter_by_distance(
    rows: list[dict], max_distance: float, key: str = "distance"
) -> list[dict]:
    return [r for r in rows if r[key] <= max_distance]


def compute_schema_sha(columns: list[tuple]) -> str:
    canon = "\n".join(sorted(f"{t}.{c}:{d}" for t, c, d in columns))
    return hashlib.sha256(canon.encode()).hexdigest()
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `.venv/bin/pytest tests/test_improve_decisions.py -q`
Expected: 9 passed

- [ ] **Step 5: Commit**

```bash
git add src/cityops_harness/improve.py tests/test_improve_decisions.py
git commit -m "feat: merge/lifecycle/harvest/threshold decision helpers

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 3: State registry entries for notebook 01

**Files:**
- Modify: `src/cityops_harness/state.py` (extend `REGISTRY`)
- Test: `tests/test_state.py` (append one test)

**Interfaces:**
- Consumes: `ArtifactCheck`, `REGISTRY` from `state.py`.
- Produces: `REGISTRY["01_self_improving_copilot"]` with four checks (domain data rows; the three HARNESS registry tables exist). Notebook 02 will call `require(conn, "01_self_improving_copilot")`.

- [ ] **Step 1: Write the failing test** (append to `tests/test_state.py`)

```python
def test_registry_seeds_notebook_01():
    checks = REGISTRY["01_self_improving_copilot"]
    descs = " ".join(c.description for c in checks)
    sqls = " ".join(c.sql for c in checks)
    assert len(checks) == 4
    assert "CITY_INSPECTION_FINDING" in sqls
    assert "HARNESS_TOOLS" in sqls
    assert "HARNESS_WORKFLOW" in sqls
    assert "HARNESS_SKILLS" in sqls
    assert "domain data" in descs
```

- [ ] **Step 2: Run test to verify it fails**

Run: `.venv/bin/pytest tests/test_state.py::test_registry_seeds_notebook_01 -q`
Expected: FAIL with `KeyError: '01_self_improving_copilot'`

- [ ] **Step 3: Extend `REGISTRY` in `src/cityops_harness/state.py`**

Add after the `"00_setup"` entry (keep the trailing comment about later notebooks):

```python
    "01_self_improving_copilot": [
        ArtifactCheck(
            description="CityOps domain data loaded (CITY_INSPECTION_FINDING rows)",
            sql="SELECT COUNT(*) FROM CITY_INSPECTION_FINDING",
        ),
        ArtifactCheck(
            description="tool registry table HARNESS_TOOLS",
            sql="SELECT COUNT(*) FROM user_tables WHERE table_name = 'HARNESS_TOOLS'",
        ),
        ArtifactCheck(
            description="workflow capture table HARNESS_WORKFLOW",
            sql="SELECT COUNT(*) FROM user_tables WHERE table_name = 'HARNESS_WORKFLOW'",
        ),
        ArtifactCheck(
            description="skillbox table HARNESS_SKILLS",
            sql="SELECT COUNT(*) FROM user_tables WHERE table_name = 'HARNESS_SKILLS'",
        ),
    ],
```

- [ ] **Step 4: Run the full suite**

Run: `.venv/bin/pytest -q`
Expected: all pass (50 = 34 prior + 16 new from Tasks 1–3).

- [ ] **Step 5: Commit**

```bash
git add src/cityops_harness/state.py tests/test_state.py
git commit -m "feat: artifact checks for notebook 01

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 4: Notebook generator A — bootstrap and registries

**Files:**
- Create: `tools/make_01_notebook.py` (sections: intro, setup, domain bootstrap, registry DDL — later tasks append more sections to this file)

**Interfaces:**
- Consumes: `.superpowers/sdd/phase01-api-reference.md` (READ IT FIRST — the bootstrap cells below are adapted from its verbatim lab code; do not deviate from its oracledb/vector patterns).
- Produces: a runnable generator whose `SECTIONS` list later tasks extend; cell-level names the rest of the notebook relies on: `settings, conn, embedder (OracleONNXEmbedder), CHAT (chat model), HANDLER (langfuse or None), _asset_names, get_asset(asset_id), log_finding(...), find_similar_findings(...), ASSET_A, ASSET_B`. Registry tables `HARNESS_TOOLS/WORKFLOW/SKILLS` created idempotently.

- [ ] **Step 1: Write `tools/make_01_notebook.py`**

```python
"""Regenerate notebooks/01_self_improving_copilot_complete.ipynb.

Run: .venv/bin/python tools/make_01_notebook.py
Later plan tasks append sections; keep SECTIONS ordered.
"""

import nbformat as nbf


def md(src):
    return nbf.v4.new_markdown_cell(src)


def code(src):
    return nbf.v4.new_code_cell(src)


# --------------------------------------------------------------------------
# Section: intro
# --------------------------------------------------------------------------
INTRO = [
    md(
        "# 01 - The Self-Improving Copilot\n\n"
        "The CityOps copilot you built in the prerequisite lab could *advise*. "
        "In this notebook it learns to *act* - and to get better at acting:\n\n"
        "1. **Tool registry** - its capabilities become rows it retrieves by meaning\n"
        "2. **Agent loop** - the model calls tools; every call is captured as a full trajectory\n"
        "3. **Verified success** - an LLM judge audits each run against the database record; "
        "only verified runs count (no more `success = bool(final)`) \n"
        "4. **Workflow dedup** - recurring intents merge via an embedding gray band + LLM confirmation\n"
        "5. **Skill harvest & lifecycle** - what recurs *and verifiably works* is distilled into a "
        "`SKILL.md`, born **provisional**, and earns **approved** (or **retired**) through linked outcomes\n\n"
        "Each mechanism closes a gap documented in `memory_promotion_review.md`: promotion without "
        "curation, success without verification, growth without forgetting."
    ),
]

# --------------------------------------------------------------------------
# Section: setup (settings, DB, embedder, chat model, tracing, state gate)
# --------------------------------------------------------------------------
SETUP = [
    md("## 0 · Setup\n\nConnect, verify notebook 00's artifacts, and stand up the in-database embedder."),
    code(
        "import array\n"
        "import json\n"
        "import uuid\n"
        "\n"
        "from cityops_harness.checks import ok, check\n"
        "from cityops_harness.config import load_settings\n"
        "from cityops_harness.db import get_connection\n"
        "from cityops_harness.llm import chat_model\n"
        "from cityops_harness.state import require, verify\n"
        "from cityops_harness.tracing import init_tracing\n"
        "from cityops_harness import improve\n"
        "\n"
        "settings = load_settings()\n"
        "conn = get_connection(settings)\n"
        "require(conn, \"00_setup\")\n"
        "CHAT = chat_model(settings)\n"
        "HANDLER = init_tracing(settings)\n"
        "ok(f\"connected - provider={settings.llm_provider}, langfuse={settings.langfuse_mode}\")"
    ),
    code(
        "# The same IEmbedder bridge the CityOps lab uses: embeddings are computed\n"
        "# inside the database via VECTOR_EMBEDDING() - no text egress, no API cost.\n"
        "import asyncio\n"
        "import numpy as np\n"
        "from langchain_oracledb.embeddings.oracleai import OracleEmbeddings\n"
        "from oracleagentmemory.apis.embedders.embedder import IEmbedder\n"
        "\n"
        "\n"
        "class OracleONNXEmbedder(IEmbedder):\n"
        "    def __init__(self, conn, model_name):\n"
        "        self.provider = OracleEmbeddings(\n"
        "            conn=conn, params={\"provider\": \"database\", \"model\": model_name}\n"
        "        )\n"
        "\n"
        "    def embed(self, texts, *, is_query=False):\n"
        "        return np.asarray(self.provider.embed_documents(list(texts)), dtype=np.float32)\n"
        "\n"
        "    async def embed_async(self, texts, *, is_query=False):\n"
        "        return await asyncio.to_thread(self.embed, texts, is_query=is_query)\n"
        "\n"
        "\n"
        "embedder = OracleONNXEmbedder(conn, settings.embed_model)\n"
        "check(embedder.embed([\"hello\"]).shape == (1, 384), \"in-database embedder returns 384-dim vectors\")"
    ),
]

# --------------------------------------------------------------------------
# Section: domain bootstrap (idempotent - CITY_ASSET, CITY_INSPECTION_FINDING)
# --------------------------------------------------------------------------
BOOTSTRAP = [
    md(
        "## 1 · Domain bootstrap (pre-built)\n\n"
        "The system of record from the CityOps lab, loaded idempotently: the asset registry and\n"
        "~inspection findings with in-database embeddings. Re-running this section is safe."
    ),
    code(
        "from pathlib import Path\n"
        "\n"
        "_data_dir = Path(\"data\") if Path(\"data\").exists() else Path(\"../data\")\n"
        "with open(_data_dir / \"maintenance_logs.json\") as f:\n"
        "    _logs = json.load(f)\n"
        "with open(_data_dir / \"inspection_reports.json\") as f:\n"
        "    _reports = json.load(f)\n"
        "_asset_names = sorted({x[\"asset_name\"] for x in _logs} | {x[\"asset_name\"] for x in _reports})\n"
        "\n"
        "\n"
        "def classify_asset(name: str) -> str:\n"
        "    n = name.lower()\n"
        "    for key, cls in ((\"bridge\", \"bridge\"), (\"tower\", \"tower\"), (\"pump\", \"pump_station\"),\n"
        "                     (\"tunnel\", \"tunnel\"), (\"plant\", \"plant\"), (\"station\", \"station\")):\n"
        "        if key in n:\n"
        "            return cls\n"
        "    return \"structure\"\n"
        "\n"
        "\n"
        "def _table_exists(name: str) -> bool:\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(\"SELECT COUNT(*) FROM user_tables WHERE table_name = :t\", t=name)\n"
        "        return cur.fetchone()[0] > 0\n"
        "\n"
        "\n"
        "if not _table_exists(\"CITY_ASSET\"):\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(\"\"\"CREATE TABLE CITY_ASSET (\n"
        "            asset_id     VARCHAR2(128) PRIMARY KEY,\n"
        "            asset_class  VARCHAR2(32) NOT NULL,\n"
        "            created_at   TIMESTAMP DEFAULT CURRENT_TIMESTAMP)\"\"\")\n"
        "        cur.executemany(\"INSERT INTO CITY_ASSET (asset_id, asset_class) VALUES (:1, :2)\",\n"
        "                        [(a, classify_asset(a)) for a in _asset_names])\n"
        "    conn.commit()\n"
        "\n"
        "\n"
        "def get_asset(asset_id: str):\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(\"SELECT asset_id, asset_class FROM CITY_ASSET WHERE asset_id = :id\", id=asset_id)\n"
        "        row = cur.fetchone()\n"
        "    return {\"asset_id\": row[0], \"asset_class\": row[1]} if row else None\n"
        "\n"
        "ok(f\"{len(_asset_names)} assets registered\")"
    ),
    code(
        "import os\n"
        "\n"
        "DEMO_FINDING_COUNT = int(os.getenv(\"DEMO_FINDING_COUNT\", \"60\"))\n"
        "\n"
        "if not _table_exists(\"CITY_INSPECTION_FINDING\"):\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(\"\"\"CREATE TABLE CITY_INSPECTION_FINDING (\n"
        "            finding_id      VARCHAR2(64) PRIMARY KEY,\n"
        "            asset_id        VARCHAR2(128) NOT NULL,\n"
        "            inspector       VARCHAR2(128),\n"
        "            overall_grade   VARCHAR2(2),\n"
        "            category        VARCHAR2(32),\n"
        "            severity        VARCHAR2(16),\n"
        "            description     CLOB NOT NULL,\n"
        "            recommendation  CLOB,\n"
        "            days_ago        NUMBER,\n"
        "            embedding       VECTOR(384) NOT NULL,\n"
        "            created_at      TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n"
        "            CONSTRAINT fk_finding_asset FOREIGN KEY (asset_id) REFERENCES CITY_ASSET(asset_id))\"\"\")\n"
        "        try:\n"
        "            cur.execute(\"\"\"CREATE VECTOR INDEX city_finding_embedding_idx\n"
        "                ON CITY_INSPECTION_FINDING (embedding)\n"
        "                ORGANIZATION INMEMORY NEIGHBOR GRAPH DISTANCE COSINE\n"
        "                WITH TARGET ACCURACY 95 PARAMETERS (TYPE HNSW, M 16, EFCONSTRUCTION 200)\"\"\")\n"
        "        except Exception as e:\n"
        "            print(f\"  (skipped HNSW index: {e})\")\n"
        "    conn.commit()\n"
        "\n"
        "with conn.cursor() as cur:\n"
        "    cur.execute(\"SELECT COUNT(*) FROM CITY_INSPECTION_FINDING\")\n"
        "    _existing = cur.fetchone()[0]\n"
        "\n"
        "if _existing == 0:\n"
        "    rows = []\n"
        "    for report in _reports:\n"
        "        if len(rows) >= DEMO_FINDING_COUNT:\n"
        "            break\n"
        "        for finding in report[\"findings\"]:\n"
        "            if len(rows) >= DEMO_FINDING_COUNT:\n"
        "                break\n"
        "            vec = array.array('f', embedder.embed([finding[\"description\"]])[0].tolist())\n"
        "            rows.append({\n"
        "                \"finding_id\": str(uuid.uuid4())[:12], \"asset_id\": report[\"asset_name\"],\n"
        "                \"inspector\": report[\"inspector\"], \"overall_grade\": report[\"overall_grade\"],\n"
        "                \"category\": finding[\"category\"], \"severity\": finding[\"severity\"],\n"
        "                \"description\": finding[\"description\"], \"recommendation\": finding[\"recommendation\"],\n"
        "                \"days_ago\": report[\"days_ago\"], \"embedding\": vec,\n"
        "            })\n"
        "    with conn.cursor() as cur:\n"
        "        cur.executemany(\"\"\"INSERT INTO CITY_INSPECTION_FINDING\n"
        "            (finding_id, asset_id, inspector, overall_grade, category, severity,\n"
        "             description, recommendation, days_ago, embedding)\n"
        "            VALUES (:finding_id, :asset_id, :inspector, :overall_grade, :category, :severity,\n"
        "                    :description, :recommendation, :days_ago, :embedding)\"\"\", rows)\n"
        "    conn.commit()\n"
        "    _existing = len(rows)\n"
        "ok(f\"{_existing} findings in the system of record\")"
    ),
    code(
        "def log_finding(asset_id, inspector, category, severity, description,\n"
        "                recommendation=\"\", overall_grade=None, days_ago=0):\n"
        "    finding_id = str(uuid.uuid4())[:12]\n"
        "    vec = array.array('f', embedder.embed([description])[0].tolist())\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(\"\"\"INSERT INTO CITY_INSPECTION_FINDING\n"
        "             (finding_id, asset_id, inspector, overall_grade, category, severity,\n"
        "              description, recommendation, days_ago, embedding)\n"
        "           VALUES (:finding_id, :asset_id, :inspector, :overall_grade, :category, :severity,\n"
        "                   :description, :recommendation, :days_ago, :embedding)\"\"\",\n"
        "            finding_id=finding_id, asset_id=asset_id, inspector=inspector,\n"
        "            overall_grade=overall_grade, category=category, severity=severity,\n"
        "            description=description, recommendation=recommendation,\n"
        "            days_ago=days_ago, embedding=vec)\n"
        "    conn.commit()\n"
        "    return finding_id\n"
        "\n"
        "\n"
        "def find_similar_findings(description, asset_id=None, category=None, k=3):\n"
        "    query_vec = array.array('f', embedder.embed([description])[0].tolist())\n"
        "    sql = \"\"\"SELECT finding_id, asset_id, inspector, overall_grade, category, severity,\n"
        "                    description, recommendation, days_ago,\n"
        "                    VECTOR_DISTANCE(embedding, :q, COSINE) AS score\n"
        "               FROM CITY_INSPECTION_FINDING\n"
        "              WHERE (:asset_id IS NULL OR asset_id = :asset_id)\n"
        "                AND (:category IS NULL OR category = :category)\n"
        "              ORDER BY score FETCH FIRST :k ROWS ONLY\"\"\"\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(sql, q=query_vec, asset_id=asset_id, category=category, k=k)\n"
        "        cols = [d[0].lower() for d in cur.description]\n"
        "        out = []\n"
        "        for r in cur.fetchall():\n"
        "            row = dict(zip(cols, r))\n"
        "            for key in (\"description\", \"recommendation\"):\n"
        "                v = row.get(key)\n"
        "                if v is not None and hasattr(v, \"read\"):\n"
        "                    row[key] = v.read()\n"
        "            out.append(row)\n"
        "    return out\n"
        "\n"
        "ASSET_A = \"Harbor Bridge\" if \"Harbor Bridge\" in _asset_names else _asset_names[0]\n"
        "ASSET_B = next(a for a in _asset_names if a != ASSET_A)\n"
        "ok(f\"domain tools ready - demo assets: {ASSET_A!r}, {ASSET_B!r}\")"
    ),
]

# --------------------------------------------------------------------------
# Section: registry DDL (idempotent)
# --------------------------------------------------------------------------
REGISTRY_DDL = [
    md(
        "## 2 · The harness registries\n\n"
        "Three plain tables the harness *writes to as it learns*:\n\n"
        "- `HARNESS_TOOLS` - capabilities, retrieved by meaning\n"
        "- `HARNESS_WORKFLOW` - captured trajectories with **verified** outcome counts\n"
        "- `HARNESS_SKILLS` - distilled `SKILL.md` documents with a **lifecycle** "
        "(provisional → approved | retired), linked back to outcomes via `skill_id`"
    ),
    code(
        "for ddl in [\n"
        "    \"\"\"CREATE TABLE HARNESS_TOOLS (\n"
        "        name           VARCHAR2(64) PRIMARY KEY,\n"
        "        description    VARCHAR2(1000) NOT NULL,\n"
        "        params_schema  CLOB,\n"
        "        embedding      VECTOR(384) NOT NULL,\n"
        "        created_at     TIMESTAMP DEFAULT CURRENT_TIMESTAMP)\"\"\",\n"
        "    \"\"\"CREATE TABLE HARNESS_WORKFLOW (\n"
        "        workflow_id        VARCHAR2(64) PRIMARY KEY,\n"
        "        intent             VARCHAR2(2000) NOT NULL,\n"
        "        intent_embedding   VECTOR(384) NOT NULL,\n"
        "        steps              CLOB NOT NULL,\n"
        "        tools_used         VARCHAR2(1000),\n"
        "        occurrences        NUMBER DEFAULT 1,\n"
        "        verified_successes NUMBER DEFAULT 0,\n"
        "        failures           NUMBER DEFAULT 0,\n"
        "        promoted           VARCHAR2(1) DEFAULT 'N',\n"
        "        last_seen          TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n"
        "        created_at         TIMESTAMP DEFAULT CURRENT_TIMESTAMP)\"\"\",\n"
        "    \"\"\"CREATE TABLE HARNESS_SKILLS (\n"
        "        skill_id           VARCHAR2(64) PRIMARY KEY,\n"
        "        name               VARCHAR2(128) NOT NULL,\n"
        "        description        VARCHAR2(1000) NOT NULL,\n"
        "        content            CLOB NOT NULL,\n"
        "        sha                VARCHAR2(64) NOT NULL,\n"
        "        status             VARCHAR2(16) DEFAULT 'provisional'\n"
        "                           CHECK (status IN ('provisional','approved','retired')),\n"
        "        schema_sha         VARCHAR2(64),\n"
        "        source_workflow_id VARCHAR2(64),\n"
        "        embedding          VECTOR(384) NOT NULL,\n"
        "        uses               NUMBER DEFAULT 0,\n"
        "        verified_successes NUMBER DEFAULT 0,\n"
        "        failures           NUMBER DEFAULT 0,\n"
        "        created_at         TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\n"
        "        updated_at         TIMESTAMP DEFAULT CURRENT_TIMESTAMP)\"\"\",\n"
        "]:\n"
        "    table = ddl.split(\"CREATE TABLE \")[1].split(\" \")[0].strip()\n"
        "    if not _table_exists(table):\n"
        "        with conn.cursor() as cur:\n"
        "            cur.execute(ddl)\n"
        "        conn.commit()\n"
        "ok(\"HARNESS_TOOLS / HARNESS_WORKFLOW / HARNESS_SKILLS ready\")"
    ),
    code(
        "def current_schema_sha():\n"
        "    \"\"\"Fingerprint of the domain schema a skill was distilled against.\n"
        "    Skills whose stored schema_sha no longer matches are flagged stale at retrieval.\"\"\"\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(\"\"\"SELECT table_name, column_name, data_type\n"
        "                         FROM user_tab_columns\n"
        "                        WHERE table_name LIKE 'CITY!_%' ESCAPE '!'\n"
        "                           OR table_name LIKE 'HARNESS!_%' ESCAPE '!'\"\"\")\n"
        "        cols = cur.fetchall()\n"
        "    return improve.compute_schema_sha(cols)\n"
        "\n"
        "SCHEMA_SHA = current_schema_sha()\n"
        "ok(f\"schema fingerprint {SCHEMA_SHA[:12]}...\")"
    ),
]

SECTIONS = [INTRO, SETUP, BOOTSTRAP, REGISTRY_DDL]


def build() -> nbf.NotebookNode:
    nb = nbf.v4.new_notebook()
    nb.metadata["kernelspec"] = {
        "name": "harness",
        "display_name": "CityOps Harness",
        "language": "python",
    }
    nb["cells"] = [cell for section in SECTIONS for cell in section]
    return nb


if __name__ == "__main__":
    nbf.write(build(), "notebooks/01_self_improving_copilot_complete.ipynb")
    print("wrote notebooks/01_self_improving_copilot_complete.ipynb")
```

- [ ] **Step 2: Generate and validate**

Run:
```bash
.venv/bin/python tools/make_01_notebook.py && .venv/bin/python - <<'EOF'
import ast, json
nb = json.load(open('notebooks/01_self_improving_copilot_complete.ipynb'))
n = 0
for c in nb['cells']:
    if c['cell_type'] == 'code':
        compile(''.join(c['source']), '<cell>', 'exec', flags=ast.PyCF_ALLOW_TOP_LEVEL_AWAIT)
        n += 1
assert nb['metadata']['kernelspec']['name'] == 'harness'
print('compiled', n, 'code cells')
EOF
```
Expected: `wrote ...` then `compiled 7 code cells`.

- [ ] **Step 3: Commit**

```bash
git add tools/make_01_notebook.py notebooks/01_self_improving_copilot_complete.ipynb
git commit -m "feat: notebook 01 generator - bootstrap and harness registries

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 5: Notebook generator B — tools, agent loop, judge, capture

**Files:**
- Modify: `tools/make_01_notebook.py` (insert new sections into `SECTIONS` after `REGISTRY_DDL`)

**Interfaces:**
- Consumes: cell names from Task 4 (`conn, embedder, CHAT, HANDLER, log_finding, find_similar_findings, get_asset, ASSET_A/B, SCHEMA_SHA`) and `improve` helpers from Tasks 1–2.
- Produces: cell-level names Task 6 relies on: `TOOLBOX` (dict name → LangChain tool), `retrieve_tools(query, k=4)`, `run_copilot_task(task, inspector="Mercer", use_skills=True) -> dict` (keys `answer, trajectory, skill_ids`), `judge_workflow(task, run) -> Verdict` (`verified: bool, reason: str`), `record_score(name, value, comment="")`, `capture_workflow(intent, run, verified) -> str` (workflow_id). Contains TODOs 1–4.

- [ ] **Step 1: Append the sections to `tools/make_01_notebook.py`**

Insert this code before the `SECTIONS = ...` line, and change that line to
`SECTIONS = [INTRO, SETUP, BOOTSTRAP, REGISTRY_DDL, TOOLBOX_SEC, AGENT_LOOP, JUDGE, CAPTURE]`.

```python
# --------------------------------------------------------------------------
# Section: toolbox (register + retrieve by meaning)
# --------------------------------------------------------------------------
TOOLBOX_SEC = [
    md(
        "## 3 · The toolbox: capabilities retrieved by meaning\n\n"
        "Each LangChain tool is registered with an embedded description. At run time the\n"
        "agent loop retrieves only the tools relevant to the task - **with a distance\n"
        "threshold**, so an off-topic task injects nothing (one of the review's fixes)."
    ),
    code(
        "from langchain_core.tools import tool\n"
        "\n"
        "CURRENT_INSPECTOR = \"R_Mercer\"\n"
        "\n"
        "\n"
        "@tool\n"
        "def tool_find_similar_findings(description: str, asset_id: str = None, k: int = 3) -> str:\n"
        "    \"\"\"Search past inspection findings semantically similar to a description.\n"
        "    Returns finding_id, inspector, severity, recommendation and distance per hit.\"\"\"\n"
        "    hits = find_similar_findings(description, asset_id=asset_id, k=k)\n"
        "    return json.dumps([\n"
        "        {k2: v for k2, v in h.items() if k2 != 'embedding'} for h in hits\n"
        "    ], default=str)\n"
        "\n"
        "\n"
        "@tool\n"
        "def tool_log_finding(asset_id: str, category: str, severity: str,\n"
        "                     description: str, recommendation: str = \"\") -> str:\n"
        "    \"\"\"Record a new inspection finding in the system of record.\n"
        "    Returns the new finding_id. Use only after checking similar past findings.\"\"\"\n"
        "    return log_finding(asset_id=asset_id, inspector=CURRENT_INSPECTOR, category=category,\n"
        "                       severity=severity, description=description, recommendation=recommendation)\n"
        "\n"
        "\n"
        "@tool\n"
        "def tool_get_asset(asset_id: str) -> str:\n"
        "    \"\"\"Look up an asset's registry record (asset_id, asset_class).\"\"\"\n"
        "    return json.dumps(get_asset(asset_id))\n"
        "\n"
        "\n"
        "TOOLBOX = {t.name: t for t in [tool_find_similar_findings, tool_log_finding, tool_get_asset]}\n"
        "ok(f\"{len(TOOLBOX)} tools defined\")"
    ),
    code(
        "def register_tool(t):\n"
        "    text = f\"{t.name}: {t.description}\"\n"
        "    vec = array.array('f', embedder.embed([text])[0].tolist())\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(\"DELETE FROM HARNESS_TOOLS WHERE name = :n\", n=t.name)\n"
        "        cur.execute(\"\"\"INSERT INTO HARNESS_TOOLS (name, description, params_schema, embedding)\n"
        "                       VALUES (:n, :d, :p, :e)\"\"\",\n"
        "                    n=t.name, d=t.description, p=json.dumps(t.args), e=vec)\n"
        "    conn.commit()\n"
        "\n"
        "\n"
        "for _t in TOOLBOX.values():\n"
        "    register_tool(_t)\n"
        "\n"
        "TOOL_DISTANCE_MAX = 0.75\n"
        "\n"
        "\n"
        "def retrieve_tools(query: str, k: int = 4):\n"
        "    qv = array.array('f', embedder.embed([query])[0].tolist())\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(\"\"\"SELECT name, description, VECTOR_DISTANCE(embedding, :q, COSINE) AS distance\n"
        "                         FROM HARNESS_TOOLS ORDER BY distance FETCH FIRST :k ROWS ONLY\"\"\",\n"
        "                    q=qv, k=k)\n"
        "        rows = [dict(zip([d[0].lower() for d in cur.description], r)) for r in cur.fetchall()]\n"
        "    # ✏️ TODO(1): apply the distance threshold so irrelevant tools are never injected.\n"
        "    #             Use improve.filter_by_distance(rows, TOOL_DISTANCE_MAX).\n"
        "    # TODO-SOLUTION-START\n"
        "    rows = improve.filter_by_distance(rows, TOOL_DISTANCE_MAX)\n"
        "    # TODO-SOLUTION-END\n"
        "    return rows\n"
        "\n"
        "\n"
        "_hits = retrieve_tools(\"corrosion on bridge bearings - check history and record it\")\n"
        "check(len(_hits) >= 2, f\"task-relevant retrieval: {[h['name'] for h in _hits]}\")\n"
        "check(retrieve_tools(\"what is the weather tomorrow in Paris?\") == [],\n"
        "      \"off-topic query injects no tools\")"
    ),
]

# --------------------------------------------------------------------------
# Section: agent loop with full-trajectory capture
# --------------------------------------------------------------------------
AGENT_LOOP = [
    md(
        "## 4 · The agent loop: act, and remember *exactly* what you did\n\n"
        "The review's sharpest finding: the original harness captured a **sorted set of tool\n"
        "names** - nothing to distill from. Here every step records the tool, its **arguments**,\n"
        "and a **truncated result**, in order. That trajectory is what the judge audits and\n"
        "what skills are distilled from."
    ),
    code(
        "from langchain_core.messages import AIMessage, HumanMessage, SystemMessage, ToolMessage\n"
        "\n"
        "AGENT_SYSTEM_PROMPT = (\n"
        "    \"You are the CityOps inspection copilot. You act through tools.\\n\"\n"
        "    \"Rules: check similar past findings BEFORE recording a new one; base severity on\\n\"\n"
        "    \"evidence; when you record a finding, cite its finding_id in your answer.\\n\"\n"
        "    \"Finish with a concise summary (<= 6 sentences).\"\n"
        ")\n"
        "\n"
        "\n"
        "def run_copilot_task(task: str, inspector: str = \"R_Mercer\", use_skills: bool = True,\n"
        "                     max_iters: int = 6) -> dict:\n"
        "    global CURRENT_INSPECTOR\n"
        "    CURRENT_INSPECTOR = inspector\n"
        "    manifest, skill_ids = (build_skill_manifest(task) if use_skills else (\"\", []))\n"
        "    tools_meta = retrieve_tools(task)\n"
        "    lc_tools = [TOOLBOX[t[\"name\"]] for t in tools_meta if t[\"name\"] in TOOLBOX]\n"
        "    model = CHAT.bind_tools(lc_tools) if lc_tools else CHAT\n"
        "    cfg = {\"callbacks\": [HANDLER]} if HANDLER else {}\n"
        "    msgs = [SystemMessage(content=AGENT_SYSTEM_PROMPT + manifest), HumanMessage(content=task)]\n"
        "    trajectory = []\n"
        "    resp = None\n"
        "    for _ in range(max_iters):\n"
        "        resp = model.invoke(msgs, config=cfg)\n"
        "        msgs.append(resp)\n"
        "        if not getattr(resp, \"tool_calls\", None):\n"
        "            break\n"
        "        for tc in resp.tool_calls:\n"
        "            result = str(TOOLBOX[tc[\"name\"]].invoke(tc[\"args\"]))\n"
        "            # ✏️ TODO(2): capture this step for the trajectory - append a dict with\n"
        "            #             the tool name, its args, and improve.truncate_result(result).\n"
        "            # TODO-SOLUTION-START\n"
        "            trajectory.append({\"tool\": tc[\"name\"], \"args\": tc[\"args\"],\n"
        "                               \"result\": improve.truncate_result(result)})\n"
        "            # TODO-SOLUTION-END\n"
        "            msgs.append(ToolMessage(content=result, tool_call_id=tc[\"id\"]))\n"
        "    return {\"answer\": resp.content if resp is not None else \"\",\n"
        "            \"trajectory\": trajectory, \"skill_ids\": skill_ids}\n"
        "\n"
        "\n"
        "def build_skill_manifest(query):  # placeholder until Section 6 redefines it\n"
        "    return \"\", []\n"
        "\n"
        "ok(\"agent loop defined\")"
    ),
]

# --------------------------------------------------------------------------
# Section: the judge (verified success)
# --------------------------------------------------------------------------
JUDGE = [
    md(
        "## 5 · Verified success: the judge audits the database, not the prose\n\n"
        "`success = bool(final)` let confident nonsense count as success. Here an LLM judge\n"
        "sees the task, the full trajectory, the final answer, **and the database evidence**\n"
        "(claimed writes are re-queried). Only a verified run feeds the workflow's success count -\n"
        "and every verdict is logged to Langfuse as a score when tracing is on."
    ),
    code(
        "from pydantic import BaseModel\n"
        "\n"
        "\n"
        "class Verdict(BaseModel):\n"
        "    verified: bool\n"
        "    reason: str\n"
        "\n"
        "\n"
        "JUDGE_PROMPT = (\n"
        "    \"You are auditing one run of an inspection copilot.\\n\"\n"
        "    \"Mark verified=true ONLY if: (a) the trajectory shows the copilot actually did what\\n\"\n"
        "    \"the task required, (b) every write it claims is confirmed by the database evidence,\\n\"\n"
        "    \"and (c) the final answer is consistent with the trajectory. Otherwise verified=false.\\n\"\n"
        "    \"Be strict: an answer that merely SOUNDS complete does not pass.\\n\\n\"\n"
        "    \"# Task\\n{task}\\n\\n# Trajectory\\n{trajectory}\\n\\n# Final answer\\n{answer}\\n\\n\"\n"
        "    \"# Database evidence\\n{evidence}\"\n"
        ")\n"
        "\n"
        "\n"
        "def build_evidence(trajectory):\n"
        "    lines = []\n"
        "    for step in trajectory:\n"
        "        if step[\"tool\"] == \"tool_log_finding\":\n"
        "            fid = step[\"result\"].strip()\n"
        "            with conn.cursor() as cur:\n"
        "                cur.execute(\"\"\"SELECT asset_id, category, severity FROM CITY_INSPECTION_FINDING\n"
        "                               WHERE finding_id = :f\"\"\", f=fid)\n"
        "                row = cur.fetchone()\n"
        "            if row:\n"
        "                lines.append(f\"finding {fid} EXISTS: asset={row[0]} category={row[1]} severity={row[2]}\")\n"
        "            else:\n"
        "                lines.append(f\"finding {fid} NOT FOUND in CITY_INSPECTION_FINDING\")\n"
        "    return \"\\n\".join(lines) or \"(no database writes claimed)\"\n"
        "\n"
        "\n"
        "_judge_model = CHAT.with_structured_output(Verdict)\n"
        "\n"
        "\n"
        "def judge_workflow(task: str, run: dict) -> Verdict:\n"
        "    prompt = JUDGE_PROMPT.format(\n"
        "        task=task,\n"
        "        trajectory=improve.trajectory_to_text(run[\"trajectory\"]),\n"
        "        answer=run[\"answer\"],\n"
        "        evidence=build_evidence(run[\"trajectory\"]),\n"
        "    )\n"
        "    cfg = {\"callbacks\": [HANDLER]} if HANDLER else {}\n"
        "    return _judge_model.invoke(prompt, config=cfg)\n"
        "\n"
        "\n"
        "def record_score(name: str, value: float, comment: str = \"\"):\n"
        "    \"\"\"Log a verdict as a Langfuse score; silently no-ops when tracing is off.\"\"\"\n"
        "    if HANDLER is None:\n"
        "        return\n"
        "    try:\n"
        "        from langfuse import get_client\n"
        "        get_client().create_score(name=name, value=value, comment=comment)\n"
        "        get_client().flush()\n"
        "    except Exception as e:\n"
        "        print(f\"(langfuse score skipped: {e})\")\n"
        "\n"
        "ok(\"judge ready\")"
    ),
]

# --------------------------------------------------------------------------
# Section: workflow capture with gray-band dedup
# --------------------------------------------------------------------------
CAPTURE = [
    md(
        "## 6 · Workflow capture: dedup by meaning, with an honest gray band\n\n"
        "A fixed 0.15 cosine cutoff under-merges paraphrases and over-merges neighbours.\n"
        "Here distances split three ways: certain merge, certain new, and a **gray band**\n"
        "where a cheap LLM call answers 'same task?' before counts are polluted."
    ),
    code(
        "class SameTask(BaseModel):\n"
        "    same: bool\n"
        "\n"
        "\n"
        "_same_task_model = CHAT.with_structured_output(SameTask)\n"
        "\n"
        "\n"
        "def _nearest_workflow(intent_vec):\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(\"\"\"SELECT workflow_id, intent,\n"
        "                              VECTOR_DISTANCE(intent_embedding, :q, COSINE) AS distance\n"
        "                         FROM HARNESS_WORKFLOW ORDER BY distance FETCH FIRST 1 ROWS ONLY\"\"\",\n"
        "                    q=intent_vec)\n"
        "        row = cur.fetchone()\n"
        "    if row is None:\n"
        "        return None\n"
        "    return {\"workflow_id\": row[0],\n"
        "            \"intent\": row[1].read() if hasattr(row[1], \"read\") else row[1],\n"
        "            \"distance\": row[2]}\n"
        "\n"
        "\n"
        "def capture_workflow(intent: str, run: dict, verified: bool) -> str:\n"
        "    intent_vec = array.array('f', embedder.embed([intent])[0].tolist())\n"
        "    nearest = _nearest_workflow(intent_vec)\n"
        "    decision = \"new\"\n"
        "    if nearest is not None:\n"
        "        # ✏️ TODO(3): decide merge/new. Use improve.merge_decision(nearest['distance']);\n"
        "        #             on 'ask', confirm with the LLM: are `intent` and `nearest['intent']`\n"
        "        #             the same recurring task? (_same_task_model.invoke(...).same)\n"
        "        # TODO-SOLUTION-START\n"
        "        decision = improve.merge_decision(nearest[\"distance\"])\n"
        "        if decision == \"ask\":\n"
        "            q = (f\"Task A: {intent}\\nTask B: {nearest['intent']}\\n\"\n"
        "                 \"Are these the same recurring task type (same procedure, possibly \"\n"
        "                 \"different asset)? Answer same=true/false.\")\n"
        "            decision = \"merge\" if _same_task_model.invoke(q).same else \"new\"\n"
        "        # TODO-SOLUTION-END\n"
        "    if decision == \"merge\":\n"
        "        wid = nearest[\"workflow_id\"]\n"
        "        with conn.cursor() as cur:\n"
        "            # ✏️ TODO(4): only a VERIFIED run may increment verified_successes; an\n"
        "            #             unverified run increments failures. Update occurrences,\n"
        "            #             the right counter, steps (latest verified trajectory), last_seen.\n"
        "            # TODO-SOLUTION-START\n"
        "            if verified:\n"
        "                cur.execute(\"\"\"UPDATE HARNESS_WORKFLOW\n"
        "                               SET occurrences = occurrences + 1,\n"
        "                                   verified_successes = verified_successes + 1,\n"
        "                                   steps = :s, last_seen = CURRENT_TIMESTAMP\n"
        "                             WHERE workflow_id = :w\"\"\",\n"
        "                            s=json.dumps(run[\"trajectory\"], default=str), w=wid)\n"
        "            else:\n"
        "                cur.execute(\"\"\"UPDATE HARNESS_WORKFLOW\n"
        "                               SET occurrences = occurrences + 1,\n"
        "                                   failures = failures + 1,\n"
        "                                   last_seen = CURRENT_TIMESTAMP\n"
        "                             WHERE workflow_id = :w\"\"\", w=wid)\n"
        "            # TODO-SOLUTION-END\n"
        "        conn.commit()\n"
        "        return wid\n"
        "    wid = str(uuid.uuid4())[:12]\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(\"\"\"INSERT INTO HARNESS_WORKFLOW\n"
        "                       (workflow_id, intent, intent_embedding, steps, tools_used,\n"
        "                        verified_successes, failures)\n"
        "                       VALUES (:w, :i, :e, :s, :t, :vs, :f)\"\"\",\n"
        "                    w=wid, i=intent, e=intent_vec,\n"
        "                    s=json.dumps(run[\"trajectory\"], default=str),\n"
        "                    t=\",\".join(sorted({s[\"tool\"] for s in run[\"trajectory\"]})),\n"
        "                    vs=1 if verified else 0, f=0 if verified else 1)\n"
        "    conn.commit()\n"
        "    return wid\n"
        "\n"
        "\n"
        "def run_and_learn(task: str, **kwargs) -> dict:\n"
        "    \"\"\"One full cycle: act -> judge -> record verdict -> capture -> update skill outcomes.\"\"\"\n"
        "    run = run_copilot_task(task, **kwargs)\n"
        "    verdict = judge_workflow(task, run)\n"
        "    record_score(\"workflow_verified\", 1.0 if verdict.verified else 0.0, verdict.reason)\n"
        "    wid = capture_workflow(task, run, verdict.verified)\n"
        "    update_skill_outcomes(run[\"skill_ids\"], verdict.verified)\n"
        "    print(f\"verified={verdict.verified} workflow={wid}\\n  judge: {verdict.reason[:140]}\")\n"
        "    return {\"run\": run, \"verdict\": verdict, \"workflow_id\": wid}\n"
        "\n"
        "\n"
        "def update_skill_outcomes(skill_ids, verified):  # redefined in Section 7\n"
        "    pass\n"
        "\n"
        "ok(\"capture pipeline ready\")"
    ),
]
```

- [ ] **Step 2: Regenerate and validate**

Run the same generate+compile command as Task 4 Step 2.
Expected: `compiled 12 code cells`.

- [ ] **Step 3: Commit**

```bash
git add tools/make_01_notebook.py notebooks/01_self_improving_copilot_complete.ipynb
git commit -m "feat: notebook 01 - toolbox, agent loop, judge, gray-band capture

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 6: Notebook generator C — harvester, lifecycle, demo arc

**Files:**
- Modify: `tools/make_01_notebook.py` (append sections; final `SECTIONS` order: `INTRO, SETUP, BOOTSTRAP, REGISTRY_DDL, TOOLBOX_SEC, AGENT_LOOP, JUDGE, CAPTURE, SKILLS_SEC, DEMO, CLOSING`)
- Create (generated): `notebooks/01_self_improving_copilot_todo.ipynb`

**Interfaces:**
- Consumes: everything from Tasks 4–5 (`run_and_learn`, `capture_workflow`, `TOOLBOX`, `SCHEMA_SHA`, `improve.*`). Redefines the Task-5 placeholders `build_skill_manifest` and `update_skill_outcomes` with real implementations (same signatures).
- Produces: the complete notebook + `_todo` flavor. Contains TODO 5.

- [ ] **Step 1: Append to `tools/make_01_notebook.py`**

```python
# --------------------------------------------------------------------------
# Section: harvester + skill lifecycle + two-level retrieval
# --------------------------------------------------------------------------
SKILLS_SEC = [
    md(
        "## 7 · Harvesting skills - and making them earn their authority\n\n"
        "The harvester promotes workflows that **recur and verifiably work** into `SKILL.md`\n"
        "documents. Three review fixes land here:\n\n"
        "- distillation reads the **full trajectory** (args + results), then a **faithfulness\n"
        "  check** compares the distilled steps back against it - unfaithful skills are rejected\n"
        "- a new skill is born **provisional** (low prompt authority) and records the\n"
        "  **schema fingerprint** it was distilled against\n"
        "- retrieval applies a **distance threshold**, injects **approved before provisional**,\n"
        "  and flags **stale** skills whose schema no longer matches"
    ),
    code(
        "class Distilled(BaseModel):\n"
        "    name: str\n"
        "    description: str\n"
        "    when_to_use: str\n"
        "    steps_body: str\n"
        "\n"
        "\n"
        "class Faithful(BaseModel):\n"
        "    faithful: bool\n"
        "    reason: str\n"
        "\n"
        "\n"
        "_distill_model = CHAT.with_structured_output(Distilled)\n"
        "_faithful_model = CHAT.with_structured_output(Faithful)\n"
        "\n"
        "\n"
        "def promote_workflow_to_skill(row) -> str | None:\n"
        "    steps_text = improve.trajectory_to_text(json.loads(row[\"steps\"]))\n"
        "    d = _distill_model.invoke(\n"
        "        \"Distill this verified, recurring copilot trajectory into a reusable procedure.\\n\"\n"
        "        \"Ground every step in what ACTUALLY happened - name the tools used, in order,\\n\"\n"
        "        \"with parameterised arguments. Do not invent steps.\\n\\n\"\n"
        "        f\"Intent: {row['intent']}\\n\\nTrajectory:\\n{steps_text}\"\n"
        "    )\n"
        "    f = _faithful_model.invoke(\n"
        "        \"Do these distilled steps faithfully describe this trajectory - same tools, same\\n\"\n"
        "        \"order, no invented actions? faithful=true/false.\\n\\n\"\n"
        "        f\"Distilled steps:\\n{d.steps_body}\\n\\nTrajectory:\\n{steps_text}\"\n"
        "    )\n"
        "    if not f.faithful:\n"
        "        print(f\"  REJECTED (unfaithful distillation): {f.reason[:120]}\")\n"
        "        return None\n"
        "    tools_used = sorted({s['tool'] for s in json.loads(row['steps'])})\n"
        "    content = improve.render_skill_md(\n"
        "        name=d.name, description=d.description, tools=tools_used,\n"
        "        when_to_use=d.when_to_use, steps_body=d.steps_body,\n"
        "        source_workflow_id=row[\"workflow_id\"], schema_sha=SCHEMA_SHA)\n"
        "    import hashlib\n"
        "    sha = hashlib.sha256(content.encode()).hexdigest()\n"
        "    sid = str(uuid.uuid4())[:12]\n"
        "    vec = array.array('f', embedder.embed([f\"{d.name}: {d.description}\"])[0].tolist())\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(\"\"\"INSERT INTO HARNESS_SKILLS\n"
        "                       (skill_id, name, description, content, sha, status,\n"
        "                        schema_sha, source_workflow_id, embedding)\n"
        "                       VALUES (:sid, :n, :d, :c, :sha, 'provisional', :ss, :src, :e)\"\"\",\n"
        "                    sid=sid, n=d.name, d=d.description, c=content, sha=sha,\n"
        "                    ss=SCHEMA_SHA, src=row[\"workflow_id\"], e=vec)\n"
        "        cur.execute(\"UPDATE HARNESS_WORKFLOW SET promoted = 'Y' WHERE workflow_id = :w\",\n"
        "                    w=row[\"workflow_id\"])\n"
        "    conn.commit()\n"
        "    print(f\"  harvested PROVISIONAL skill {d.name!r} ({sid}) from workflow {row['workflow_id']}\")\n"
        "    return sid\n"
        "\n"
        "\n"
        "def harvest() -> list:\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(\"\"\"SELECT workflow_id, intent, steps, occurrences,\n"
        "                              verified_successes, failures\n"
        "                         FROM HARNESS_WORKFLOW WHERE promoted = 'N'\"\"\")\n"
        "        cols = [d[0].lower() for d in cur.description]\n"
        "        rows = []\n"
        "        for r in cur.fetchall():\n"
        "            row = dict(zip(cols, r))\n"
        "            if hasattr(row[\"steps\"], \"read\"):\n"
        "                row[\"steps\"] = row[\"steps\"].read()\n"
        "            if hasattr(row[\"intent\"], \"read\"):\n"
        "                row[\"intent\"] = row[\"intent\"].read()\n"
        "            rows.append(row)\n"
        "    harvested = []\n"
        "    for row in rows:\n"
        "        # ✏️ TODO(5): the harvest gate - promote only what recurs AND verifiably works.\n"
        "        #             Use improve.harvest_ready(occurrences, verified_successes, failures).\n"
        "        # TODO-SOLUTION-START\n"
        "        if not improve.harvest_ready(row[\"occurrences\"], row[\"verified_successes\"],\n"
        "                                     row[\"failures\"]):\n"
        "            continue\n"
        "        # TODO-SOLUTION-END\n"
        "        sid = promote_workflow_to_skill(row)\n"
        "        if sid:\n"
        "            harvested.append(sid)\n"
        "    return harvested\n"
        "\n"
        "ok(\"harvester ready\")"
    ),
    code(
        "SKILL_DISTANCE_MAX = 0.55\n"
        "\n"
        "\n"
        "def retrieve_skills(query: str, k: int = 3):\n"
        "    qv = array.array('f', embedder.embed([query])[0].tolist())\n"
        "    with conn.cursor() as cur:\n"
        "        cur.execute(\"\"\"SELECT skill_id, name, status, content, schema_sha,\n"
        "                              VECTOR_DISTANCE(embedding, :q, COSINE) AS distance\n"
        "                         FROM HARNESS_SKILLS WHERE status != 'retired'\n"
        "                        ORDER BY distance FETCH FIRST :k ROWS ONLY\"\"\", q=qv, k=k)\n"
        "        cols = [d[0].lower() for d in cur.description]\n"
        "        rows = []\n"
        "        for r in cur.fetchall():\n"
        "            row = dict(zip(cols, r))\n"
        "            if hasattr(row[\"content\"], \"read\"):\n"
        "                row[\"content\"] = row[\"content\"].read()\n"
        "            rows.append(row)\n"
        "    return improve.filter_by_distance(rows, SKILL_DISTANCE_MAX)\n"
        "\n"
        "\n"
        "def build_skill_manifest(query):\n"
        "    \"\"\"Two-level retrieval: approved skills carry authority; provisional ones are\n"
        "    labelled as unproven; stale schema fingerprints are flagged.\"\"\"\n"
        "    hits = retrieve_skills(query)\n"
        "    if not hits:\n"
        "        return \"\", []\n"
        "    cur_sha = current_schema_sha()\n"
        "    parts, ids = [], []\n"
        "    for h in sorted(hits, key=lambda r: 0 if r[\"status\"] == \"approved\" else 1):\n"
        "        ids.append(h[\"skill_id\"])\n"
        "        stale = \" [STALE: schema changed since distillation - re-verify]\" \\\n"
        "            if h[\"schema_sha\"] != cur_sha else \"\"\n"
        "        if h[\"status\"] == \"approved\":\n"
        "            label = f\"APPROVED procedure{stale} - prefer this approach:\"\n"
        "        else:\n"
        "            label = (f\"PROVISIONAL procedure{stale} - an unproven candidate; treat as a\"\n"
        "                     \" suggestion and verify against fresh data:\")\n"
        "        parts.append(f\"\\n\\n--- {label} ---\\n{h['content']}\")\n"
        "    return \"\".join(parts), ids\n"
        "\n"
        "\n"
        "def update_skill_outcomes(skill_ids, verified):\n"
        "    \"\"\"Close the loop the review said never closes: runs that used a skill feed the\n"
        "    skill's own record, and the lifecycle transition runs on the new counts.\"\"\"\n"
        "    for sid in skill_ids:\n"
        "        with conn.cursor() as cur:\n"
        "            cur.execute(\"\"\"UPDATE HARNESS_SKILLS SET uses = uses + 1,\n"
        "                           verified_successes = verified_successes + :vs,\n"
        "                           failures = failures + :f, updated_at = CURRENT_TIMESTAMP\n"
        "                         WHERE skill_id = :sid\"\"\",\n"
        "                        vs=1 if verified else 0, f=0 if verified else 1, sid=sid)\n"
        "            cur.execute(\"\"\"SELECT status, verified_successes, failures\n"
        "                             FROM HARNESS_SKILLS WHERE skill_id = :sid\"\"\", sid=sid)\n"
        "            status, vs, fl = cur.fetchone()\n"
        "            new_status = improve.lifecycle_transition(status, vs, fl)\n"
        "            if new_status != status:\n"
        "                cur.execute(\"UPDATE HARNESS_SKILLS SET status = :s WHERE skill_id = :sid\",\n"
        "                            s=new_status, sid=sid)\n"
        "                print(f\"  skill {sid}: {status} -> {new_status}\")\n"
        "        conn.commit()\n"
        "\n"
        "ok(\"skill retrieval + lifecycle ready\")"
    ),
]

# --------------------------------------------------------------------------
# Section: the demo arc
# --------------------------------------------------------------------------
DEMO = [
    md(
        "## 8 · The arc: recur → verify → harvest → earn approval\n\n"
        "Three inspectors phrase the same corrosion-triage task differently. Watch the gray\n"
        "band merge them, the judge verify them, and the harvester fire at three verified\n"
        "occurrences. Then a fourth task *uses* the provisional skill - and its verified\n"
        "outcomes promote it to approved."
    ),
    code(
        "print(f\"=== Occurrence 1 ({ASSET_A}) ===\")\n"
        "r1 = run_and_learn(\n"
        "    f\"Inspect {ASSET_A}: heavy corrosion on the bearing plates at pier 2. Assess severity\"\n"
        "    \" against past findings and record a finding with your recommendation.\",\n"
        "    use_skills=False)"
    ),
    code(
        "print(f\"=== Occurrence 2 ({ASSET_A}, new phrasing) ===\")\n"
        "r2 = run_and_learn(\n"
        "    f\"Corrosion check on {ASSET_A} bearings - compare with prior reports and log what\"\n"
        "    \" you find with a recommendation.\",\n"
        "    inspector=\"T_Vance\", use_skills=False)"
    ),
    code(
        "print(f\"=== Occurrence 3 ({ASSET_B}, same task type, different asset) ===\")\n"
        "r3 = run_and_learn(\n"
        "    f\"{ASSET_B} shows surface rust and section loss on load-bearing members; triage\"\n"
        "    \" against history and file a finding.\",\n"
        "    inspector=\"K_Osei\", use_skills=False)"
    ),
    code(
        "# The registry after three occurrences: one workflow row, merged by meaning.\n"
        "with conn.cursor() as cur:\n"
        "    cur.execute(\"\"\"SELECT workflow_id, occurrences, verified_successes, failures, promoted\n"
        "                     FROM HARNESS_WORKFLOW\"\"\")\n"
        "    for row in cur.fetchall():\n"
        "        print(row)"
    ),
    code(
        "print(\"=== Harvest ===\")\n"
        "new_skills = harvest()\n"
        "check(len(new_skills) >= 1, f\"harvested {len(new_skills)} provisional skill(s)\")\n"
        "with conn.cursor() as cur:\n"
        "    cur.execute(\"SELECT skill_id, name, status FROM HARNESS_SKILLS\")\n"
        "    for row in cur.fetchall():\n"
        "        print(row)"
    ),
    code(
        "# Show the distilled SKILL.md - grounded in the real trajectory, not confabulated.\n"
        "with conn.cursor() as cur:\n"
        "    cur.execute(\"SELECT content FROM HARNESS_SKILLS FETCH FIRST 1 ROWS ONLY\")\n"
        "    _content = cur.fetchone()[0]\n"
        "print(_content.read() if hasattr(_content, 'read') else _content)"
    ),
    code(
        "print(f\"=== Using the provisional skill (run 1) ===\")\n"
        "r4 = run_and_learn(\n"
        "    f\"New corrosion report on {ASSET_A}: pitting on expansion-joint anchor bolts.\"\n"
        "    \" Triage against history and record a finding.\",\n"
        "    inspector=\"J_Ibarra\")\n"
        "check(len(r4[\"run\"][\"skill_ids\"]) >= 1, \"provisional skill was retrieved and used\")"
    ),
    code(
        "print(f\"=== Using the skill again (run 2) - promotion happens here if both verified ===\")\n"
        "r5 = run_and_learn(\n"
        "    f\"{ASSET_B}: corrosion staining under deck drainage outlets. Check history, then\"\n"
        "    \" record a finding with recommendation.\",\n"
        "    inspector=\"R_Mercer\")\n"
        "with conn.cursor() as cur:\n"
        "    cur.execute(\"SELECT skill_id, name, status, uses, verified_successes FROM HARNESS_SKILLS\")\n"
        "    for row in cur.fetchall():\n"
        "        print(row)"
    ),
    code(
        "# Retrieval thresholds in action: an off-topic task injects NO skills, no tools.\n"
        "manifest, ids = build_skill_manifest(\"plan the office holiday party\")\n"
        "check(manifest == \"\" and ids == [], \"off-topic query injects no skills\")"
    ),
]

# --------------------------------------------------------------------------
# Section: closing
# --------------------------------------------------------------------------
CLOSING = [
    md(
        "## 9 · What you built (vs. what the review criticised)\n\n"
        "| Review gap | This notebook |\n"
        "|---|---|\n"
        "| Trajectory = sorted set of tool names | Ordered steps with args + truncated results |\n"
        "| `success = bool(final)` | LLM judge audits trajectory **and database evidence** |\n"
        "| Brittle 0.15 dedup cutoff | Gray band + LLM same-task confirmation |\n"
        "| One-way promotion, max authority | Provisional → approved/retired via linked outcomes |\n"
        "| No staleness handling | Schema fingerprint checked at retrieval |\n"
        "| Unconditional top-k injection | Distance thresholds on tools *and* skills |\n\n"
        "Next: **02 - Scheduled briefings**, where the *other* promotion path (scratch →\n"
        "long-term memory) gets the same treatment: opt-in curation, provenance, supersession,\n"
        "and a consumer that is actually scheduled."
    ),
    code(
        "for desc, passed in verify(conn, \"01_self_improving_copilot\"):\n"
        "    check(passed, desc)\n"
        "ok(\"notebook 01 artifacts in place - continue to 02_scheduled_briefings\")"
    ),
]
```

- [ ] **Step 2: Regenerate, validate, and produce the learner flavor**

Run:
```bash
.venv/bin/python tools/make_01_notebook.py && .venv/bin/python - <<'EOF'
import ast, json
nb = json.load(open('notebooks/01_self_improving_copilot_complete.ipynb'))
n = sum(1 for c in nb['cells'] if c['cell_type'] == 'code')
for c in nb['cells']:
    if c['cell_type'] == 'code':
        compile(''.join(c['source']), '<cell>', 'exec', flags=ast.PyCF_ALLOW_TOP_LEVEL_AWAIT)
src = json.dumps(nb)
assert src.count('TODO-SOLUTION-START') == 5, src.count('TODO-SOLUTION-START')
print('compiled', n, 'code cells; 5 TODO blocks present')
EOF
.venv/bin/python tools/make_student.py notebooks
.venv/bin/python - <<'EOF'
import json
nb = json.load(open('notebooks/01_self_improving_copilot_todo.ipynb'))
src = json.dumps(nb)
assert 'TODO-SOLUTION' not in src
assert src.count('your code here') == 5
assert src.count('\\u270f') + src.count('✏') >= 5   # instructions survive
print('learner notebook ok')
EOF
```
Expected: `compiled 24 code cells; 5 TODO blocks present`, one `01_self_improving_copilot_complete.ipynb -> 01_self_improving_copilot_todo.ipynb` line from make_student (00's file is `00_setup.ipynb`, not `*_complete.ipynb`, so it is untouched), `learner notebook ok`.

- [ ] **Step 3: Run the full test suite**

Run: `.venv/bin/pytest -q`
Expected: 50 passed.

- [ ] **Step 4: Commit**

```bash
git add tools/make_01_notebook.py notebooks/01_self_improving_copilot_complete.ipynb notebooks/01_self_improving_copilot_todo.ipynb
git commit -m "feat: notebook 01 - harvester, skill lifecycle, demo arc, learner flavor

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
```

---

### Task 7: Live end-to-end execution and publish

**Files:**
- Modify (only if the live run exposes bugs): `tools/make_01_notebook.py` + regenerate both flavors
- Modify: `README.md` (mark notebook 01 available in the series table)

**Interfaces:**
- Consumes: populated `.env` (already live-verified in notebook 00), the committed notebook.
- Produces: an executed, verified notebook; pushed branch.

- [ ] **Step 1: Reset any partial HARNESS state from earlier attempts**

Only these tables; never touch CITY_ or other tables:

```bash
.venv/bin/python - <<'EOF'
from cityops_harness.config import load_settings
from cityops_harness.db import get_connection

conn = get_connection(load_settings())
with conn.cursor() as cur:
    for t in ("HARNESS_TOOLS", "HARNESS_WORKFLOW", "HARNESS_SKILLS"):
        try:
            cur.execute(f"DROP TABLE {t} PURGE")
            print("dropped", t)
        except Exception:
            print("absent", t)
conn.commit()
EOF
```

- [ ] **Step 2: Execute the notebook live**

Run:
```bash
.venv/bin/jupyter nbconvert --to notebook --execute notebooks/01_self_improving_copilot_complete.ipynb \
  --output /private/tmp/claude-501/-Users-anantsr-Documents-Projects-total-recall/11d74471-e897-46f3-8e21-bbfa42b4c813/scratchpad/01_executed.ipynb 2>&1 | tail -5
```
Expected: `Writing ... bytes` with no traceback. This calls the live LLM ~15–20 times (agent runs + judges + distillation); allow up to 15 minutes. If a cell fails: fix the *generator*, regenerate both flavors, reset per Step 1, re-run. LLM-dependent checks (`check(len(new_skills) >= 1, ...)`, skill-used check) may legitimately fail on a bad model day — if a `check` fails, read the executed notebook's outputs, diagnose whether it is a code bug or model variance, and report it; do not weaken checks to pass.

- [ ] **Step 3: Inspect the executed outputs**

```bash
.venv/bin/python - <<'EOF'
import json, re
nb = json.load(open('/private/tmp/claude-501/-Users-anantsr-Documents-Projects-total-recall/11d74471-e897-46f3-8e21-bbfa42b4c813/scratchpad/01_executed.ipynb'))
for cell in nb['cells']:
    for out in cell.get('outputs', []):
        html = ''.join(out.get('data', {}).get('text/html', []))
        text = ''.join(out.get('text', []))
        for s in (html, text):
            s = re.sub(r'<[^>]+>', '', s).strip()
            if s:
                print(s)
EOF
```

Verify in the printed outputs: `verified=True` on at least 2 of the first 3 runs, `harvested PROVISIONAL skill`, a lifecycle line `provisional -> approved` (or explain why not), both off-topic checks green (`off-topic query injects no tools` / `no skills`), final state.verify summary green.

- [ ] **Step 4: Update README series table**

In `README.md`, change the series row for 01 from a bare name to
`| `notebooks/01_self_improving_copilot_complete.ipynb` (+ `_todo`) | Tool registry, verified workflow capture, skill harvesting with lifecycle |`
(keep the other rows as they are).

- [ ] **Step 5: Full suite, commit, push**

```bash
.venv/bin/pytest -q
git add README.md tools/make_01_notebook.py notebooks/
git commit -m "feat: notebook 01 verified live end-to-end

Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>"
git push
```

---

## Post-plan checklist

- `git status`: no secrets staged; `notebook_complete_ollama.ipynb` untouched.
- Carry-overs to phase 02: SDK (`oracleagentmemory`) init against ADB with `CITY_` prefix is still unexercised — notebook 02 does it first; Langfuse modes still untested live; state registry entries for 02.
